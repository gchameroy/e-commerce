version: "3"

services:
  app:
    ports:
      - "10010:80"

  db:
    ports:
       - "10011:3306"

  phpmyadmin:
      image: phpmyadmin/phpmyadmin
      depends_on:
          - db
      ports:
          - "10012:80"
      environment:
          MYSQL_ROOT_PASSWORD: root
          PMA_HOST: db
